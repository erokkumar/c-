#include<bits/stdc++.h>
using namespace std;

int max(int arr[] , int n){
     sort(arr, arr + n);
     for (int i = 0; i < n; i++)
     {
          
     }
     
}

int maint(){
     int t;
     cin>>t;
     while (t--)
     {
          int n;
          cin>>n;
          int arr[n];
          for (int i = 0; i < n; i++)
          {
               cin>>arr[i];
          }    
     }
}